/* Write your T-SQL query statement below */

--https://leetcode.com/problems/customers-who-never-order/description/

select cust.name as Customers
from
    Customers cust
        left outer join
    Orders ord on cust.id = ord.customerId
where ord.customerId is null 